SA_PASSWORD=
SESSION_SECRET=
FRED_API_KEY=
# Cloudflare Named Tunnel token(必填)
# 到 https://one.dash.cloudflare.com/ → Zero Trust → Networks → Tunnels
# 建立一個 tunnel,複製 Install/run command 裡那個 eyJhIjoi... 開頭的字串貼到這裡
TUNNEL_TOKEN=